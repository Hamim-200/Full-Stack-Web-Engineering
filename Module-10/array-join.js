const numbs = [1, 3, 5, 2, 8];
const joined = numbs.join(' -> ') //1 -> 3 -> 5 -> 2 -> 8
console.log(joined)

// const position = numbs.indexOf(28);
// console.log('position of 5', position)

// console.log(Array.isArray([45]));