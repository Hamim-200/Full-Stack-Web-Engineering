// Abstraction in TypeScript : Abstraction means hiding unnecessary implementation details and showing only the important parts.


// Think about a car You know: start(), stop(), drive() But you don't need to know exactly how the engine works internally.That's abstraction.


// Math.floor()
// Math.ceil()
// Math.random()

const d = new Date()
console.log(d)